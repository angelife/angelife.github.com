# Angelife 网站视觉重构记录

> 重构日期：2026-06-13
> 目标风格：Digital Garden / naosense 气质
> 关键词：minimal, essay-first, knowledge base, slow web

---

## 一、修改文件清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `layouts/index.html` | 重写 | 极简名片首页：站点标题 + 副标题 + 五行横排入口 + 底栏链接 |
| `layouts/_default/single.html` | 重写 | 文章页：标题+日期+系列标签 + 缩略封面 + 正文 + 标签。兼容 `cover` map/string 两种 frontmatter 格式 |
| `layouts/_default/list.html` | 重写 | 列表页（系列/分类）：标题 + 纯列表（标题+日期），无卡片/摘要/配图 |
| `layouts/_default/archives.html` | 新建 | 归档页：每行 `标题 日期`，极简行式，仿 naosense |
| `css/angelife-brand.css` | 重写 | 全新 CSS 体系，详见下方 |
| `hugo-site/hugo.toml` | 修改 | 移除 main menu（导航精简至 Logo 纯文本 + 主题切换按钮） |

---

## 二、CSS 颜色系统

| 变量 | 值 | 用途 |
|------|-----|------|
| `--al-ink` | `#222` | 正文主色 |
| `--al-secondary` | `#666` | 辅助文字 |
| `--al-bg` | `#FAFAF8` | 页面背景（暖灰白） |
| `--al-border` | `#e8e6e1` | 分割线 |
| `--al-link` | `#4a5568` | 链接（低饱和蓝灰） |
| `--al-quote-line` | `#4a5568` | 引用块左边线 |
| `--al-quote-bg` | `#f4f3f0` | 引用块背景 |
| `--al-code-bg` | `#f0efe9` | 代码背景 |
| `--al-tag-bg` | `#eceae5` | 标签背景 |

**删除**：旧的五元素质感色块（金/木/水/火/土彩虹色）、渐变、科技蓝等所有装饰色。

---

## 三、字体与排版

| 元素 | 字体 | 字号 | 行距 |
|------|------|------|------|
| 正文 | Noto Serif SC, LXGW WenKai | 20px | 1.9 |
| 英文 | Inter, Georgia | - | - |
| 代码 | JetBrains Mono, Fira Code | 0.85em | - |
| 段落间距 | - | - | 1.4em |
| 内容宽度 | - | 820px | - |
| 正文行高 | - | 1.9 | - |

---

## 四、页面风格对照

### 首页
| 旧 | 新 |
|----|----|
| 5个五行彩色卡片 + 文章卡片网格 | 居中名片式：标题"安知生" + 副标题 + 五行横排5个链接 |
| 50vh 彩色卡片，彩虹色 | 极简，无背景色，无装饰 |
| 大量文章卡片 | 无文章列表（文章通过系列页/归档访问） |

### 文章页
| 旧 | 新 |
|----|----|
| PaperMod 默认单篇样式 | 标题大字 + 日期 + 系列标签 + 280px 高度封面 + 820px 正文 |
| 面包屑导航 | 移除面包屑 |
| 社交分享按钮 | 移除 |
| 上一篇/下一篇 | 移除 |
| 评论框(giscus) | 保留但默认关闭 |
| 阅读时间 | 已关闭 |

### 归档页
| 旧 | 新 |
|----|----|
| 404（不存在） | 每行 `标题 日期`，灰色日期，虚线下划线 |

### 系列/分类页
| 旧 | 新 |
|----|----|
| 带封面的文章卡片网格 | 标题 + 列表（标题左对齐，日期右对齐），无配图无摘要 |

### 导航栏
| 旧 | 新 |
|----|----|
| 11个菜单项（文章 + 5五行 + 反民粹 + 搜索 + 关于） | 仅 Logo "安知生" 文字 + 主题切换按钮 |
| 实色白底 | 毛玻璃效果 `rgba(250,250,248,0.88) + backdrop-filter: blur(8px)` |

---

## 五、风险说明

### 低风险
- **SEO**：Meta tags、Schema.org 结构化数据完全未修改，通过 `head.html` 和 `templates/` 子模板管理，PaperMod 原逻辑不变。
- **URL 结构**：所有 permalink 规则、页面 URL 不变。
- **内容**：所有 Markdown 源文件未修改。
- **RSS**：输出格式不变。

### 中风险
- **列表页分页**：`list.html` 重构后，所有系列页（金/木/水/火/土/反民粹）和分类页都走新模板，需要逐一确认分页和排序是否正确。
- **归档页分组**：当前是全局所有文章平铺列表。未来可按年份分组（如 naosense 的 `2024`, `2023` 分组）。
- **首页 SEO**：首页不再有文章列表，`summaryLength` 不再影响首页摘要，但 `home` 的 schema.org 仍然输出。

### 需手动验证
1. 首页 `http://192.168.2.106:1313/` — 名片布局是否正常
2. 五行系列页（如 `/series/information-judgment/`）— 列表样式是否正确
3. 归档页 `/archives/` — 列表是否正常显示
4. 文章页 — 封面图是否正常渲染（兼容 cover map/string）
5. 移动端适配 — 响应式断点 `@media (max-width: 768px)`
6. 搜索页 — 未修改 search 模板，应保持正常
7. Dark mode — CSS 变量在 body.dark 下是否兼容

---

## 六、设计决策

1. **导航完全移除**：选择"名片式首页"，不保留顶栏菜单。用户通过首页五行入口或归档页找到内容。
2. **列表页不显示摘要**：遵循 naosense 风格，仅标题+日期。摘要仅出现在文章页内。
3. **封面图保持但缩小**：280px max-height，不破坏 PaperMod 的 cover 机制。
4. **保留 PaperMod 主题**：不改主题，仅通过 layouts override + CSS 实现风格转换。

---

## 七、下一步建议

- [ ] 用户在 dev server 上实际浏览，反馈各页面视觉感受
- [ ] 归档页可按年份分组（仿 naosense）
- [ ] 考虑首页底部加一行 "最近文章"（最多5篇，纯标题链接）
- [ ] 确认 dark mode 下配色是否协调
