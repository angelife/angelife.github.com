# angelife 网站风格规范

## 总体风格

- 克制、安静、长期主义、书页感。
- 文章排版借鉴 yangzhiping.com 的阅读体验，但不照抄。
- 首页可以宽屏、有气象，像系统入口和长期作品总览。
- 文章页必须窄栏、耐读、少干扰。
- 不做赛博朋克，不做过度动画，不做营销式堆叠。

## 首页

- 保持当前 full-width 修复。
- 首页承担系统入口功能，不要随意改成居中博客页。
- 五行栏目负责长期主题。
- 火·AI 卡片要能看到最新重点 AI 文章。
- 不随意大改首页结构。
- 首页可有横向画卷式 Hero、五行主卡片、各栏目精选、最新发声和关于本站。

## 文章页

- 普通文章页宽度约 `720px-780px`。
- 中文正文约 `18px / 1.9`。
- 标题清楚，description 和 meta 信息减噪。
- 段落间距舒展。
- h2 / h3 留足上边距。
- blockquote 简洁，有轻左边线即可。
- 图片克制，不要超出正文宽度太多。
- PaperMod 有 cover 时，正文不要重复插入同一张 `cover.png`。

## 栏目

- 五行栏目：金·判断、木·蝉识、水·易理、火·AI、土·正见。
- 日课：每日文章与即时思考。
- 搜索：盘活知识库。
- 更新日志：记录网站演化。
- 建站模式日志：公开说明建站与发布方法。

## 搜索

- 搜索页应像知识库入口，不做花哨效果。
- 结果需要显示标题、命中词高亮和上下文摘要。
- 标题、摘要、分类、标签命中应优先于正文深处命中。
- 无结果时显示友好提示。
- 移动端搜索框和结果不得横向溢出。

## 评论

- 评论系统优先采用 giscus，基于 GitHub Discussions。
- 未配置 `repoId` / `categoryId` 时不显示评论区。
- 正式长文可用 `comments: true` 开启。
- 日课短文默认不开，必要时再开启。
- 旧日志 / 资料归档不开。
- 不使用 Disqus，不引入明显拖慢文章页的大型评论系统。

## 配图规则

- 重点长文必须有 cover。
- 首页推荐文章必须有 cover。
- 火·AI / 系统主控 / 方法论文章建议有 cover。
- 日课可用专属 cover；没有就用分类默认图或纯文字。
- 旧日志 / 资料归档不强制配图。
- 列表页不得出现破图。

## 修改边界

- 首页和文章页是不同体验，不要互相污染。
- 首页宽屏 CSS 只作用于首页专用 class。
- 文章页窄栏 CSS 只作用于 `article.post-single` / `.post-content` 等文章上下文。
- 不要改主题源码 `themes/`，优先使用 layout override、CSS override 和内容层配置。

## 版本号

`v2026.05.27-05` 及以前为日期流水版本；自 `v0.6.0` 起，angelife 网站改用 SemVer：`vMAJOR.MINOR.PATCH`。
